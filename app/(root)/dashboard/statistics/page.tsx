import React from "react";

const page = () => {
  return (
    <div>
      Statistika kataloga / statistika klijenata / statistika prodaje /
      Logovanje / logovi zaliha proizvoda / Izverstaj o aktivnostima / Neuspesno
      preuzimanje slika
    </div>
  );
};

export default page;
